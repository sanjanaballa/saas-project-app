<%= form_with(model: arifact, local: true) do |form| %>
  <% if arifact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(arifact.errors.count, "error") %> prohibited this arifact from being saved:</h2>

      <ul>
      <% arifact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
